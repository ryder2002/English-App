'use server';
/**
 * @fileOverview This file is intentionally left empty.
 *
 * Text-to-Speech (TTS) functionality has been moved to the client-side using the
 * browser's built-in Web Speech API (window.speechSynthesis). This provides
 * instant, offline-capable audio playback without needing server-side processing
 * or external API calls for this feature.
 *
 * All components that require audio playback now implement this client-side logic
 * directly.
 */
